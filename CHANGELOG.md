# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.4.1] - 2026-09-23

### Changed（设置页）

- 不再内置 Solo Manager 默认账号：去掉 `admin` 兜底和示例 IP 占位，用户名为空时保存被拒绝、自动登录直接报错。
- 设置页刷新后立即回显当前地址和用户名：`GET /api/settings` 只读本地配置，连接检测拆到
  `GET /api/settings/connection` 异步进行，不再阻塞表单（此前每个候选地址最多等 5 秒）。

## [0.4.0] - 2026-09-23

### Added（无人值守）

- 磁盘门禁：剩余空间低于 30 GB 时暂停启动新任务，恢复后自动继续；队列页「占用概览」显示磁盘剩余。
- 自动清理：已完成超过 6 小时、无人持有、无进程的任务目录，删除 `node_modules` 与 `monitor/verify`、已退出容器，
  轨迹 / 视频 / Excel / 源码保留。后台线程分批进行，不阻塞纠错循环。线上首轮演算：62 个任务，约 40.7 GB。

### Changed（无人值守）

- `pauseOnStart` 默认改为 `crash-loop`：重启后沿用上次状态，15 分钟内异常退出 2 次以上才暂停（旧的 `true` / `false`
  对应 `always` / `never`）。
- 线上 `waitTimeoutSeconds` 4 小时 → 14 小时：历史上 39% 的正常任务超过 4 小时，worker 提前放弃后队列项变成需处理并占着名额。
- 停用旧监控台的开机自启 `com.gaobo.sologsb-monitor`（plist 改名为 `.disabled`），避免与调度台同时抢容器与配额。

### Fixed（项目锁）

- 手工入队 `queue-add-platform` 检查项目占用锁和运行中任务，与自动补队同一口径；持锁项目直接拒绝，
  读取占用状态失败时也不入队。此前一个已 `complete`、仍持锁的项目可以被直接加入队列，随后在技能 `init` 被拒绝。
- 读取候选列表和手工入队前，先释放“任务已有最终提交记录但仍持锁”的残留锁（需技能 sologsb-0917 1.3.0+）。
  本机当时有 16 个已 `QC_PASSED` 的项目仍持锁长达 24 小时。
- 队列项到达 `done` 时不释放项目锁：队列把 `gsb_ready` / `verified` 也视为终态，而审核、录屏、提交仍在进行。

### 校验记录

- `python3 -m pytest -q tests`：229 passed。

## [0.3.0] - 2026-09-23

### Added（队列页）

- 「自动补队」开关：面板移到右栏顶部，标题栏一个开关直接开启 / 关闭 `autoRefill.enabled`，
  并写回 `config.json`（`set-auto-refill`，日志 `config.auto_refill`）。开启后立即补一轮，不用等满间隔。
  面板用一句话写明当前含义（开启：待启动少于 N 项时每 60 秒补充；暂停时只补充不启动 / 关闭：只执行已有项），
  并显示上次补队结果；顶栏新增「自动补队 · 开/关」徽标。
- 「接下来启动」预览：按实际启动顺序列出前 10 个待启动项，标出等待重试会被跳过的项，每项可一键禁用
  （与队列列表共用禁用流程，禁用后从队列移除且不再补入）。

### Added（兜底策略）

- 新增 `api/guard.py`，随纠错循环每轮检查三条策略：候选阶段超时（默认 6 小时）、长时间无进展（默认 60 分钟无任何写入）、
  已结束任务的残留进程（结束 60 分钟后仍在跑，如 verify 留下的 vite）。
- 默认「仅观察」：只标记、记日志；队列页新增「兜底策略」面板（模式切换、三个阈值、命中列表），被标记的队列项显示原因。
  切到「自动终止」后：加入技能停止名单、终止任务进程、删除候选容器、状态标记 failed、队列项失败并回补配额，任务目录保留。
- `POST /api/automation` 新增 `set-guard`（`mode` / `candidatePhaseHours` / `noProgressMinutes` / `leakedProcessMinutes`）。

### Changed

- 自动补队打乱待启动顺序时保留前 10 项不动（`shuffle_pending(keep_head=…)`），预览不会每分钟变一次。
- `autoRefill` 快照新增 `intervalSeconds`、`previewSize`、`lastRun`。


### 校验记录

- `python3 -m pytest tests/ -q`：207 passed。
- `python3 -m py_compile server.py queue_worker.py queue_log.py api/*.py scripts/bump_version.py`：通过。
- `node --check static/app.js`、`zsh -n scripts/keepalive.sh`：通过。

## [0.2.2] - 2026-09-23

### Fixed

- 同项目旧任务卡死重试：技能 `init` 把工作目录里状态为 `running` / `blocked` 的同项目任务视为
  活动任务并拒绝接入，且不判断进程是否存活。09-23 00:27 因占位死锁 `blocked` 的
  `cy-381-…-001819` 让 15:44、15:48 的两次重试都在门禁上失败（各自预扣、回补一次配额）。
  调度器现在在启动某项目前检查同一工作目录中该项目的旧任务：执行器不在运行、没有候选容器、
  没有活的 `runPid`、不被活跃队列项持有、`state.json` 静默 ≥ 120 秒时标记为 `failed`
  （候选 `invalidated`），保留原状态与原因，记 `queue.stale_task_closed`。
- 队列项的提示文案会过期：自动重试的「任务连续 10 分钟无状态更新，已自动重试」在新桌面任务
  创建后仍挂着；「已静默 N 秒」在活动恢复后也不清除。现在两者都在条件消失时清掉。
- 队列页「运行容器」把免计项目（`excludedProjectCodes`）的容器也算进上限，显示成「6 / 4 已满」；
  改为只把计入上限的容器与上限对比，免计数量单独标注（`containers.counted` / `excluded`）。

### Added

- `automation.pauseOnStart`（默认开启）：每次服务启动先把队列置为暂停并写回配置，忽略上次保存的
  `paused`；由 launchd / keepalive 自动重启后不会在无人值守时拉起新任务，需在队列页手动「启动队列」。
  设置页「调度模式与上限」显示该项；日志 `config.paused_on_start`。

### Changed（队列页）

- 队列项新增服务端计算的 `phase`（`queued` / `waiting_slot` / `retrying` / `starting` /
  `executing` / `attention` / 终态），以及 `containers: {running, wanted}`、`queuePosition`、
  `monitorTaskId`；`counts.phases` 给出各阶段数量。
- 队列页按 `phase` 分组：「执行队列」只放等待中的项（需处理置顶），容器已创建、已交接执行器的
  任务移到新的「执行中」面板并显示容器数与已运行时长，可一键跳转监看页；终态项折叠到
  「已结束」组。等容器名额的项明确显示「候选 x/y 已有容器，其余在执行器内排队」。
- 每项一行「当前在做什么」，取代原来直接输出的状态机 error / notice；状态机提示只在补充信息时
  显示（静默、重试原因、跳过原因），且只有需处理 / 失败的项才用警告色。
- 容量等待的提示文案改为「容器名额已满：运行中 a + 待启动 b / 上限 n」，并带 `containerWaitKind`
  （`capacity` / `spacing` / `gate`）供页面区分。
- 技能任务状态的中文标签移到 `app.js`（`TASK_STATE_LABELS`），监看页与队列页共用。

### 校验记录（Claude Code）

校验时间 2026-09-23 16:00–16:20（UTC+8），基于工作区未提交代码，版本 0.2.2。

| 项目 | 方法 | 结果 |
|---|---|---|
| 单元 / 集成测试 | `python3 -m pytest tests/ -q` | 175 passed（新增 19 个：旧任务关闭 6、phase 8、提示清理 2、启动暂停 3） |
| 旧任务关闭前置条件 | 对线上 `sologsb-0923` 目录只读演算 | 仅 `cy-381-…-001819`（blocked，静默 15.8 h）与 `gb-12-1-…`（blocked，4.9 h）满足关闭条件；4 个 `candidates_running` 任务均有活 runPid，不会被动 |
| 页面脚本语法 | Node 逐页解析 4 个页面内联脚本与 `app.js` | 通过 |
| 界面 | 本地 mock 服务（线上快照 + 新版 `_annotate_phase` + 合成的重试 / 失败 / 需处理项）+ 无头 Chrome 截图 | 「执行队列」只剩需处理 2 + 待重试 1，「执行中」5 个任务带容器数与时长，已结束折叠 |

未覆盖：

- 线上服务已于 16:24 由 launchd（`com.gaobo.sologsb-scheduler`）重启到 0.2.2（分组 / 旧任务关闭），
  但 `pauseOnStart` 是之后加的，当前进程未包含；下次重启后队列会默认暂停。
- 旧任务关闭逻辑尚未在真实 `cy-381` 重试上运行（该项已被纠错循环释放为 skipped，等待手动重试）。

## [0.2.1] - 2026-09-23

### Fixed

- 队列页「已在队列中」的项目过滤因运算符优先级（`items || [].map(…)`）失效，已入队项目仍可选，
  点「加入队列」才被服务端拒绝；「随机选择」也会抽中已入队或已禁用的项目。两处改用同一个过滤函数，
  并随实时推送刷新。
- 服务端重复入队检查漏了 `orphaned` 状态：失联但桌面任务可能仍在跑的项目可以被再次入队。
- 监看页容器卡片把「运行中 + 排队候选」合并显示成「5 / 4」，看起来像超限；改为显示真实运行容器
  对上限，排队候选单独列出。
- 任务详情「概览」只在打开弹窗时读取一次，之后不再更新；现在在该任务有变更推送时、以及每 10 秒
  自动刷新，且不会打断正在运行的日志轮询。
- 服务重启到新版本后，已打开的页面仍使用旧的 JS/CSS；现在断线重连后检测到版本变化会自动刷新一次
  （按版本防循环），服务进程与页面版本不一致时侧栏版本号标为「待重启」。
- `/api/logs` 的 `limit` 没有上限；304 响应缺少 `Cache-Control`。

### Changed（无人值守稳定性）

- 新增 `scripts/keepalive.sh`：崩溃自动重启（指数退避）、健康检查连续异常或无响应时重启、
  `caffeinate -i` 防止 Mac 空闲睡眠、日志超过 20 MB 轮转。
- 新增进程内看门狗（`api/health.py`）：调度 / 纠错 / 快照三个后台循环上报心跳，卡住时记
  `watchdog.loop_stalled` 并附带卡住位置的调用栈，线程意外退出时自动拉起；`/api/health` 的 `ok`
  反映循环状态，设置页「运行状态」逐个显示。
- 失联候选不再长期占用容器名额：已初始化任务的候选在容器未满时超过 `phantomDemandSeconds`
  （默认 600 秒）仍拿不到容器，就不再计入占用（`queue.phantom_demand`）。今天 00:27–14:26
  容器长期只跑一半，就是 gb-538 这类失联项一直占着 2 个名额。
- Keychain 读写加 10 秒超时：屏幕锁定时 `security` 可能等待解锁弹窗，之前会让调用它的后台循环
  永久挂起。
- 服务收到 SIGTERM 时与 Ctrl-C 走同一条退出路径（记录 `service.stopped`、释放实例锁）。
- HTTP 访问日志默认只记录 4xx/5xx（`SOLOGSB_ACCESS_LOG=1` 恢复全部），避免长时间运行时
  `monitor.log` 被轮询和 SSE 请求刷满。

### 校验记录（Claude Code）

校验时间 2026-09-23 15:05–15:15（UTC+8），基于工作区未提交代码，版本 0.2.1。

| 项目 | 方法 | 结果 |
|---|---|---|
| 单元 / 集成测试 | `python3 -m pytest tests/ -q` | 156 passed（新增 18 个：看门狗与心跳 7、Keychain 超时 2、失联候选 4、重复入队 2、健康检查 / 日志上限 / 访问日志 3） |
| 测试隔离 | 测试前后对比线上 `.state/scheduler.jsonl` 行数 | 3814 → 3814，无临时目录实例写入 |
| Python 语法 | `py_compile server.py api/*.py queue_worker.py queue_log.py` | 通过 |
| 页面脚本语法 | Node 逐页解析 4 个页面内联脚本与 `app.js` | 通过 |
| 静态资源版本键 | 检查 4 个页面的 `?v=` | 均为 0.2.1 |
| 守护脚本：崩溃 | 沙箱副本（独立端口 18795、临时 state / 槽位目录、队列暂停）中 `kill -9` 服务进程 | 5 秒后自动重启，健康检查恢复 200 |
| 守护脚本：进程僵死 | 沙箱中 `kill -STOP` 冻结服务进程 | 连续 2 次健康检查无响应 → SIGTERM → 20 秒后 SIGKILL → 重启成功 |
| 守护脚本：正常停止 | 向 keepalive 发送 SIGTERM | 服务记录 `service.stopped` 后退出，无残留进程 |
| 沙箱隔离 | 对比 `~/.codex/sologsb-0917/container-limit.json` | `updatedAt` 仍为 05:56:04，未被改写 |
| 界面 | Playwright + Chrome 使用模拟 API 截图 | 容器卡片显示「4 / 4 · 排队候选 1」；设置页循环状态正确区分正常 / 连续失败 / 已卡住 |

未覆盖：

- 线上服务（pid 27392，仍运行 0.1.0 代码）未重启，新逻辑尚未在真实任务上运行。
- `queue.phantom_demand` 只在单元测试中触发，没有等到真实失联任务。
- `caffeinate` 防睡眠未做实机睡眠验证。

## [0.2.0] - 2026-09-23

### Changed

- 界面整体重设计：统一设计令牌（间距、圆角、明暗两套配色），文字改用无衬线字体，等宽字体只用于编号、
  路径和数字；最小字号从 8px 提到 11px 以上。
- 状态配色重新分工：蓝色表示运行中，琥珀色表示需处理/警告，红色表示失败，绿色表示完成；
  A/B 候选保留紫/青两色，与状态色互不混用。
- 侧栏加入导航图标，连接状态、明暗切换和版本号统一放在底部；未手动选择主题时跟随系统。
- 监看页：指标改为卡片，容器占用带进度条；筛选栏与表头合并吸顶；任务名与项目编号上下两行排列。
- 任务详情弹窗：头部操作右对齐，内容区独立滚动；概览页工作流改为时间线，候选卡片的指标改为四格。
- 队列页改为左右两栏：左侧执行队列，右侧依次为添加项目、调度上限、自动补队、占用概览、已禁用项目
  和 Prompt 模板；项目选择区底部固定显示当前选中项与「加入队列」按钮，顶栏显示队列调度/暂停状态。
- 设置页与日志页去掉与页面无关的全局指标栏。
- 窄屏（≤900px）布局：侧栏折叠为顶部导航，队列项操作按钮换行显示。

### Changed

- 容器上限统一由监控台 `maxContainers` 设置，并写入技能的 `container-limit.json`（`managedBy`
  标记优先于设备配置）；废弃 `containerRefillBelow`、`containerReserveSeconds`、`keyConcurrency`。
- 容器占用改为「运行中容器 + 活任务仍需的容器」，不再写占位标记；容器优先模式不再受最大任务数限制。
- 队列页上限表单精简为 4 项，设置页改为只读展示。
- 启动间隔最低 20 秒，每个 tick 最多启动一个任务。

### Fixed

- 日志页未导入 `renderKeyed`，调度日志始终无法显示；「自动滚动」开关也没有实际作用，现改为「跟随最新」，
  向下翻阅旧日志时自动暂停跟随。
- 日志页首屏 `/api/logs` 与 SSE 回放重叠时出现重复行，改为按 `seq` 去重。
- 设置页「调度模式与上限」把 `dt/dd` 直接放进网格，标签和值错位。
- 队列页暂停/启动、清理、切换模式失败时没有错误提示（未捕获的 Promise 异常）。
- 任务弹窗关闭后，切回标签页仍会在后台轮询候选日志。

- 终态队列项每个 tick 被重复判失败，`attempts` 膨胀到数万。
- skipped 项被复活为 orphaned 后反复释放，同一平台任务被回补 100 多次；回补改为幂等。
- 失败路径只在本地记回补，平台任务未取消；重试沿用已取消的平台任务 ID。
- 执行器退出后仍凭 `result.json` 的 stage 认定任务存活，名额被占 12 小时。
- 平台返回「已占用/运行中」时直接判失败，改为等待。
- 静默但仍在运行的任务不再改成 orphaned，`queue.stalled_held` 等日志只在状态变化时写一次。
- 测试写入线上 `.state`（日志、队列、槽位），改为跟随 `_stateDir`。
- worker 日志每行写两遍；启动失败留下空任务目录。

## [0.1.0] - 2026-09-23

### Added

- 建立 `VERSION` 单一版本源，当前版本为 `0.1.0`。
- 新增 `/api/version`，返回版本号、Git 提交和分支信息。
- 四个页面的左下角统一显示当前版本与短提交号。
- 新增 `scripts/bump_version.py`，支持 major、minor、patch 和显式版本升级。
